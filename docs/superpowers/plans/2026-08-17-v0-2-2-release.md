# v0.2.2 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish `v0.2.2` so the customer one-line installer downloads the no-elevation Windows ACL fix from the pinned source archive.

**Architecture:** Keep `v0.2.1` immutable. Bump the fixed Release contract to `v0.2.2` across the Agent helper, installer fixtures, documentation, workflow trigger, and MCP server version. Push the reviewed commit and tag; GitHub Actions builds and publishes the same six platform assets that the helper validates.

**Tech Stack:** Bash 3.2+, Windows PowerShell 5.1+, Go, GitHub Actions, GitHub Releases.

## Global Constraints

- Repository remains `Schyler0427/image2-mcp`.
- Base URL remains `https://api.schyler.top`.
- Customer still supplies only `OPENAI_IMAGE_API_KEY`.
- Release remains public, non-draft, non-prerelease, with six exact assets.
- Existing `v0.2.1` tag and Release are never force-updated.
- Verification never calls an image-generation API.

### Task 1: Bump The Fixed Release Contract

**Files:**
- Modify: `AGENT_INSTALL.md`
- Modify: `scripts/bootstrap-agent-install.sh`
- Modify: `scripts/bootstrap-agent-install.ps1`
- Modify: `scripts/setup.sh`
- Modify: `README.md`
- Modify: `.github/workflows/release.yml`
- Modify: `tests/test-agent-install-contract.sh`
- Modify: `tests/test-agent-bootstrap.sh`
- Modify: `tests/test-agent-bootstrap.ps1`
- Modify: `tests/test-install-key-only.sh`
- Modify: `tests/test-install-key-only.ps1`

- [ ] **Step 1: Replace every installer-facing `v0.2.1` reference with `v0.2.2`.**

  Update API/page/archive URLs, source roots, expected Release tags, fixture URLs, README commands, and the workflow tag trigger. Leave historical `v0.2.1` references outside the active contract unchanged unless they describe the current release process.

- [ ] **Step 2: Run the contract test and confirm it fails until all active references agree.**

  Run `bash tests/test-agent-install-contract.sh`. Expected: it reports any remaining old active version reference.

### Task 2: Bump The MCP Binary Version

**Files:**
- Modify: `cmd/image2-mcp/main.go:16`
- Modify: `cmd/image2-mcp/main_test.go:18-19`

- [ ] **Step 1: Change `serverVersion` and its test expectation from `0.2.1` to `0.2.2`.**

- [ ] **Step 2: Run `go test ./...` and confirm the version test passes.**

### Task 3: Validate Before Publishing

**Files:**
- Test: `tests/test-agent-install-contract.sh`
- Test: `tests/test-agent-bootstrap.sh`
- Test: `tests/test-install-key-only.sh`
- Test: `go test ./...`

- [ ] **Step 1: Run Bash syntax and installer tests.**

  Run `bash -n scripts/bootstrap-agent-install.sh`, `bash tests/test-agent-install-contract.sh`, `bash tests/test-agent-bootstrap.sh`, and `bash tests/test-install-key-only.sh`.

- [ ] **Step 2: Run Go tests and inspect the staged diff.**

  Run `go test ./...` and `git diff --check`. Do not create the tag until all commands exit zero.

### Task 4: Publish v0.2.2

**Files:**
- Git ref: `v0.2.2`

- [ ] **Step 1: Commit the version bump and release-contract changes.**

  Use commit message `release: prepare v0.2.2`.

- [ ] **Step 2: Push the default branch and exact tag.**

  Run `git push fork HEAD:main`, then `git tag -a v0.2.2 -m "Image2 MCP v0.2.2"`, then `git push fork v0.2.2`. Never move `v0.2.1`.

- [ ] **Step 3: Verify GitHub Actions and the public Release.**

  Require the release workflow to pass on Ubuntu and Windows, then verify the public `v0.2.2` API reports `draft=false`, `prerelease=false`, and all six exact assets. Finally fetch both raw helpers from `main` and confirm they point at `v0.2.2`.
